apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: autodeploy
spec:
  selector:
    app: nginx-sample-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30888
  type: NodePort